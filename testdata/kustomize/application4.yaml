apiVersion: skiperator.kartverket.no/v1alpha1
kind: Application
metadata:
  name: valid-manifest-4
  namespace: devex
spec:
  image: "kartverket/example"
  port: 8080
  replicas:
    min: 1
    max: 5
